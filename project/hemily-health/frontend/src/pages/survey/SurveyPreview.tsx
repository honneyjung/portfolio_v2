/**
 * 설문 프리뷰 (개발용 — 인증 없이 레이아웃 확인)
 * 확인 후 삭제 예정. HomePagePreview의 시드(authStore)를 재사용한다.
 */
import '../HomePagePreview'
import SurveyPage from './SurveyPage'

export default function SurveyPreview() {
  return <SurveyPage />
}
